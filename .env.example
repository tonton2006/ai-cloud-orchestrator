# Google Cloud Platform Configuration
# Replace these values with your actual GCP project details

# Your GCP Project ID (required)
GCP_PROJECT_ID=your-project-id

# Default GCP region for Cloud Run and other regional resources
GCP_REGION=us-central1

# Default GCP zone for Compute Engine VMs
DEFAULT_ZONE=us-central1-a

# Application Configuration
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Google Application Credentials (optional - use if not using gcloud default)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
